"""Minimal tests for the dashboard app."""

from django.test import TestCase


class DummyTestCase(TestCase):
    """Placeholder test case to ensure Django discovers the app."""

    def test_nothing(self):
        """Trivial test to satisfy the test runner."""
        self.assertTrue(True)
